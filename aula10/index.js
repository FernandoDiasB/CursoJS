const nome = 'fernando' // string
const nome1 = "fernando" // string
const nome2 = `fernando` // string
const num = 10 // number
const num1 = 10.54 // number
let nomeAluno; // undefined -> nçao aponta em local nenhum na memória
const soreNomeAluno = null // Não aponta para local nenhum na memória
const aprovado = false // boolean = true, false (valor Lógico)

